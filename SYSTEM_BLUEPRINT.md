# SYSTEM BLUEPRINT: ZODCHIY ENTERPRISE CORE (v2.0.4)

## 🎯 MASTER PROMPT ДЛЯ ВОССОЗДАНИЯ
> "Спроектируй и напиши код для PWA 'ЗОДЧИЙ CORE' на React 19 + Tailwind. 
> 1. Архитектура: No-Backend. Данные хранятся в GitHub репозитории (db.json). Связь через GitHub API. 
> 2. Синхронизация: Deep Delta Merge — объединение локального стейта (IndexedDB) и облака по timestamp (updatedAt). 
> 3. Роли: Admin (контроль штата), Manager (паспорт объекта), Foreman (задачи и фото), Supervisor (приемка). 
> 4. State Machine: Задачи переходят TODO -> IN_PROGRESS -> REVIEW (нужно фото) -> DONE или REWORK (нужен коммент). 
> 5. AI Интеграция: Используй @google/genai (Gemini 3 Flash) для анализа фотоотчетов на дефекты. 
> 6. UI/UX: Enterprise Bold, скругления 2.5rem, темная тема для Админа, Slate/Blue для остальных."

## 1. Технологический стек
- **Frontend**: React 19 (ESM), Tailwind CSS.
- **Иконки**: Lucide-React.
- **AI**: Google Gemini API (@google/genai). Модель: `gemini-3-flash-preview`.
- **Хранение**: IndexedDB (локально) + GitHub API (облако).
- **PWA**: Service Worker для Offline-доступа.

## 2. Ролевая модель
- **ADMIN**: Доступ к `AdminPanel`, управление юзерами, переключение ролей.
- **MANAGER**: Создание/редактирование `Project`, управление документацией.
- **FOREMAN**: Работа с `Task`, загрузка `Evidence` (фото), перевод в `REVIEW`.
- **SUPERVISOR**: Контроль `REVIEW`, отправка в `REWORK` с аргументацией.

## 3. Архитектура данных (Persistence)
- **Store**: Глобальный стейт `AppSnapshot`.
- **Sync Logic**: 
  - `idb.set` при каждом изменении.
  - `cloud.sync` через 2.5 сек (debounce).
  - Конфликты решаются в пользу объекта с более поздним `updatedAt`.

## 4. Визуальные стандарты
- **Layout**: Header (Logo + Sync Status), Main Content (Scrollable), Bottom Nav.
- **Cards**: Крупные, с тенями, выраженным состоянием (Border/Background).
- **Feedback**: Анимированные переходы (animate-in fade-in).

## 5. Инструкция по развертыванию
- Приложение ожидает GitHub Token и Repo в `localStorage`.
- Если данных нет, открывается Setup Mode.
- Приглашения передаются через Base64-кодированные URL параметры (`?invite=...`).
